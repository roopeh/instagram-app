export type FormInput = {
  name: string,
  label: string,
  placeholder: string,
  initialValue: string,
  type: string,
};

export type ImageFile = {
  type: string,
  size: number,
  base64: string,
};
